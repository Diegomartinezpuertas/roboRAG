# ROS 2 / colcon build artifacts
build/
install/
log/

# Local tooling / virtualenv (deps installed via requirements.txt)
.claude/
agent_env/

# Runtime data — regenerated, not committed
data/chroma_db/
data/logs/
data/zones.db
data/zones.db-shm
data/zones.db-wal
data/maps/

# Benchmark run artifacts tied to a specific SLAM map
eval/landmarks.json

# Personal promo working material — local only. demo_preflight.sh belongs
# here rather than with the launch files: it exists to stage a recording
# session (explore, seed, verify, GO/NO-GO), not to run the system.
docs/promo/
demo_preflight.sh

# Python caches
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/

# OS / editor
.DS_Store
*.swp

# Committed on purpose (documented here for clarity):
#   data/knowledge/   — static RAG knowledge base
#   eval/results/     — benchmark results (a deliverable)
#   docs/, *.md, src/ — everything else
